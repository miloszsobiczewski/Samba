version: "3"

services:
  samba:
    build:
      dockerfile: "Dockerfile"
    volumes:
      - ./Samba:/user/src/app
    ports:
      - "8000:8000"
    command: >
      python manage.py runserver 0.0.0.0:8000
